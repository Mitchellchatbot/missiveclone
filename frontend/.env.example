# Optional. Leave unset for:
#   - local dev (Vite proxies /api and /socket.io to localhost:4000)
#   - single-service Railway deploy (Express serves both API and the built UI)
# Only set this if your frontend is hosted on a *different* origin than your API.
# VITE_API_URL=https://api.example.com
